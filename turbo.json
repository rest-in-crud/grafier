{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": ["VITE_URL_BACKEND", "VITE_URL_FRONTEND"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    },
    "lint": {},
    "clean": {
      "cache": false
    }
  }
}
