{
  "name": "grafier",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "npm run format --workspaces --if-present",
    "clean": "turbo clean"
  },
  "packageManager": "npm@11.11.0",
  "devDependencies": {
    "turbo": "^2.9.16"
  },
  "overrides": {
    "zod": "^4.4.3"
  },
  "engines": {
    "node": ">=18"
  }
}